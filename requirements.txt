# TruePath AI runs on the Python 3.9+ standard library only.
# Add optional upgrades below if you want a richer similarity engine:
# scikit-learn>=1.3      # for TF-IDF based vectorization
# sentence-transformers   # for semantic embeddings
